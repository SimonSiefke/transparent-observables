{
  "name": "@relevant/cli",
  "version": "0.0.7",
  "description": "",
  "bin": {
    "relevant": "dist/index.js"
  },
  "scripts": {
    "test": "npm run build && rimraf test/dist && jest",
    "clean": "rimraf dist tsconfig.buildinfo",
    "build": "tsc",
    "build:watch": "tsc --watch"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@relevant/compiler": "^0.0.7",
    "commander": "^2.20.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/jest": "^24.0.13",
    "@types/node": "^12.0.4",
    "jest": "^24.8.0",
    "rimraf": "^2.6.3",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.1"
  }
}
